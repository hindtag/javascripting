Data Types:
    To check the type of data use typeof
        Example:
            typeof "tag"
            the output is 'string'

Variable:
    Use camelCase
        Example:
            let myName
            let thisVariableName